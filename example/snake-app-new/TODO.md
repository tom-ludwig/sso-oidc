settings:

- sensor percent trigger value
- sensor calibration values
- deepsleep time and length of sleep
- check for existing microcontrollers via ip check

dashboard:

- show sensor values (for each ip??)
- add / upload pictures for plants

- do not allow watering when no path is set

Martin der Meister:

- nur Statusmeldungen alle 5 Sekunden ändern
- alle anderen daten nur updaten bei page reload oder manuellem update